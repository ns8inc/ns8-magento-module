{
  "dependencies": {
    "@ns8/ns8-switchboard-operator": "^1.0.88"
  },
  "devDependencies": {
    "@types/node": "^12.6.2",
    "ns8-tslint": "^2.0.0",
    "serverless-pseudo-parameters": "^2.4.0",
    "serverless-step-functions": "^1.25.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "init:dev": "yarn && yarn build && yarn install-modules && yarn link @ns8/ns8-shopify-switches",
    "deploy": "sls deploy",
    "undeploy": "sls remove",
    "install-modules": "node scripts/install-modules.js",
    "build": "rm -rf dist && tsc"
  }
}
