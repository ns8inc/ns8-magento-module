{
  "dependencies": {
    "@ns8/ns8-magento2-switches": "1.0.12"
  },
  "devDependencies": {
    "aws-lambda": "0.1.2",
    "serverless-pseudo-parameters": "2.5.0",
    "serverless-step-functions": "2.10.0"
  },
  "scripts": {
    "build": "yarn install && tsc -d",
    "build:dev": "yarn build",
    "deploy": "sls deploy",
    "undeploy": "sls remove"
  }
}
