{
  "dependencies": {
    "@ns8/ns8-magento2-switches": "2.0.18"
  },
  "devDependencies": {
    "aws-lambda": "0.1.2",
    "serverless-pseudo-parameters": "2.5.0",
    "serverless-step-functions": "2.10.1",
    "typescript": "3.7.2"
  },
  "scripts": {
    "build": "yarn install && tsc -d",
    "build:dev": "yarn build && yarn link @ns8/ns8-magento2-switches",
    "deploy": "sls deploy",
    "undeploy": "sls remove"
  },
  "version": "2.0.18"
}
