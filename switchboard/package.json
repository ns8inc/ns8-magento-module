{
  "dependencies": {
    "@ns8/ns8-magento2-switches": "2.0.38"
  },
  "devDependencies": {
    "aws-lambda": "1.0.5",
    "serverless-pseudo-parameters": "2.5.0",
    "serverless-step-functions": "2.17.0",
    "typescript": "3.7.5"
  },
  "scripts": {
    "build": "yarn install && tsc -d",
    "build:dev": "yarn build && yarn link @ns8/ns8-magento2-switches",
    "clean": "rm -rf .serverless dist node_modules",
    "deploy": "sls deploy",
    "undeploy": "sls remove"
  },
  "version": "2.0.38"
}
