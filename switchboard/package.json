{
  "dependencies": {
    "@ns8/ns8-magento2-switches": "1.0.7"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.141",
    "@types/node": "^12.7.7",
    "aws-lambda": "^0.1.2",
    "lodash": "^4.17.15",
    "rimraf": "^3.0.0",
    "serverless-pseudo-parameters": "^2.4.0",
    "serverless-step-functions": "^1.25.0",
    "stacktracey": "^1.2.122",
    "typescript": "^3.6.3"
  },
  "scripts": {
    "build": "yarn compile",
    "build:dev": "yarn && yarn build",
    "compile": "rimraf dist && yarn install && tsc -d",
    "deploy": "sls deploy",
    "undeploy": "sls remove"
  }
}
