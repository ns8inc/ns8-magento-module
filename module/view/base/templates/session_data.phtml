<script type="text/javascript">
  require(['jquery'], function($) {
    var hasSessionData = <?= $block->hasSessionData() ? 'true' : 'false' ?>;

    if (!hasSessionData || true) {
      $.post('<?= $block->escapeUrl($block->url->getMagentoNS8SessionDataUrl()) ?>', {
        screenHeight: window.screen.height,
        screenWidth: window.screen.width,
        <?php if ($block->shouldAddFormKey()): ?>
        form_key: '<?= /* @noEscape */ $block->getFormKey(); ?>',
        <?php endif; ?>
      });
    }
  });
</script>
